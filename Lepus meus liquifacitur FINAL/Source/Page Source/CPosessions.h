#ifndef _C_CPos#define _C_CPos#pragma once#include "CCanvas.h"class CPosessions : public CCanvas {	public:		CPosessions (StringPtr name, const SPaneInfo &inPaneInfo,						const SViewInfo &inViewInfo);		bool isInPosessions(Point inpt);		void addItem(CShape* item);//should I be adding the CShape or the CShapeTest?								   //probably the CShape but that's something to think about		void removeItem(CShape* item);		void HighlightSelf(bool highlight);		void DrawSelf();		void UnHilite() { 			highlight = false; 			Refresh();		}	private:		int top;		bool flashed;//hack.  change 		bool highlight;};#endif